import os
from playsound import playsound

def mergeSort(arr):
    if len(arr) > 1:
        halfpoint = len(arr)//2
        l = arr[:halfpoint]
        r = arr[halfpoint:]
        
        mergeSort(l)
        mergeSort(r) 

        x = y = z = 0


